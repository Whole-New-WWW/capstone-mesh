import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  grid: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    padding: 10
  },
  dashIcon: {
    padding: 10,
    alignItems: 'center',
  },
  dashImg: {
    maxWidth: 50,
    maxHeight: 50
  }
})
